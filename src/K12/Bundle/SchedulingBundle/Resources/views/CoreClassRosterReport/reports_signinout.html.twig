{% extends 'KulaCoreFrameworkBundle::core.html.twig' %}

{% block content %}
<input type="hidden" name="record_id" value="{{ request.query.get('record_id') }}" />
<input type="hidden" name="record_type" value="{{ request.query.get('record_type') }}" />
<div class="group-box">
<div class="group-box-header">Settings for Group</div>
<div class="group-box-contents group">
  <div class="form-field"><label name="non[section_number]">Section Number for which to find group</label>
    <input type="text" name="non[section_number]" value="AAGRP-02%" size="25" /> (Wildcard is %)
  </div>
</div>
</div>

{% endblock %}